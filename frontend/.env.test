# Test env override — keeps VITE_API_BASE_URL fixed regardless of the
# developer's local .env, matching the BASE_URL hard-coded in src/test/msw/handlers/*.
VITE_API_BASE_URL=http://localhost:5080/api/v1
